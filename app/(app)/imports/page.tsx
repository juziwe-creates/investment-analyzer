import { PagePlaceholder } from "@/components/page-placeholder";

export default function ImportsPage() {
  return (
    <PagePlaceholder
      title="Imports"
      description="Manual and CSV imports will start here. Comdirect and PDF parsing are intentionally deferred."
    />
  );
}

