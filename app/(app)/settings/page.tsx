import { PagePlaceholder } from "@/components/page-placeholder";

export default function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      description="Profile, portfolio, and currency preferences will be configured here."
    />
  );
}

